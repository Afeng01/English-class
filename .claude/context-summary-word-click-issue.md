# 项目上下文摘要（单词点击问题 + 默认词库功能）
生成时间：2025-11-22

## 1. 相似实现分析

### 实现1：单词点击事件处理
**文件**：`/Users/cherry_xiao/Documents/Code-project/English-class/frontend/src/pages/ReaderPage.tsx:80-103`

**模式**：事件委托 + 异步API调用
```tsx
const handleWordClick = async (e: React.MouseEvent) => {
  const target = e.target as HTMLElement;
  const word = target.textContent?.trim();

  // 提取纯单词（去除标点）
  const cleanWord = word.replace(/[^a-zA-Z]/g, '').toLowerCase();

  // 调用词典API
  const response = await dictionaryAPI.lookup(cleanWord);
  setWordResult(response.data);
}
```

**可复用**：
- 单词清理逻辑：`word.replace(/[^a-zA-Z]/g, '').toLowerCase()`
- 弹窗定位逻辑：`setPopoverPosition({ x: e.clientX, y: e.clientY })`

**需注意**：
- 前端仅做基础清理，未处理词形变化（如：ceilings → ceiling）
- 点击事件绑定在整个内容区域（事件委托）
- 错误处理：catch中设置 `setWordResult(null)` 显示"未找到释义"

### 实现2：词典API后端实现
**文件**：`/Users/cherry_xiao/Documents/Code-project/English-class/backend/app/api/dictionary.py:11-72`

**模式**：第三方API代理模式
```python
@router.get("/{word}", response_model=DictionaryResponse)
async def lookup_word(word: str):
    # 代理 Free Dictionary API
    response = await client.get(f"https://api.dictionaryapi.dev/api/v2/entries/en/{word}")

    if response.status_code == 404:
        raise HTTPException(status_code=404, detail="Word not found")
```

**可复用**：
- 数据提取逻辑：音标、音频、释义的解析
- 错误处理机制：404、502、504

**需注意**：
- **关键发现**：后端直接透传前端传来的单词，未做词形还原处理
- 第三方API（Free Dictionary API）只支持精确匹配原形单词
- 如果传入"ceiling"（原形），API能正常返回（已验证）
- 如果传入"ceilings"（复数），API可能返回404

### 实现3：词典弹窗显示组件
**文件**：`/Users/cherry_xiao/Documents/Code-project/English-class/frontend/src/pages/ReaderPage.tsx:352-405`

**模式**：条件渲染 + 绝对定位弹窗
```tsx
{selectedWord && (
  <div className="fixed inset-0 z-30" onClick={closePopover}>
    <div style={{ top: popoverPosition.y + 10, left: popoverPosition.x - 150 }}>
      {lookupLoading ? "查询中..." : !wordResult ? "未找到释义" : <显示释义>}
    </div>
  </div>
)}
```

**需注意**：
- 状态管理清晰：`selectedWord`、`wordResult`、`lookupLoading` 三个状态
- 显示"未找到释义"时，无法区分是"API错误"还是"单词不存在"

## 2. 项目约定

### 命名约定
- 组件：PascalCase（如 `ReaderPage`）
- 函数/变量：camelCase（如 `handleWordClick`, `selectedWord`）
- API路由：小写+下划线（如 `lookup_word`）
- 常量：UPPER_SNAKE_CASE（如 `FREE_DICTIONARY_API`）

### 文件组织
```
frontend/src/
  ├── pages/          # 页面组件
  ├── services/       # API 调用服务
  ├── stores/         # 状态管理（Zustand）
  └── types/          # TypeScript 类型定义

backend/app/
  ├── api/            # API 路由
  ├── models/         # 数据库模型
  ├── schemas/        # Pydantic 数据验证
  └── services/       # 业务逻辑（目前为空）
```

### 代码风格
- 前端：React Hooks + TypeScript
- 后端：FastAPI + SQLAlchemy + Pydantic
- 异步处理：前后端都使用 async/await
- 错误处理：前端 try-catch + 状态更新，后端 HTTPException

## 3. 可复用组件清单

### 前端
- `dictionaryAPI.lookup(word)` - 词典查询API调用
- `handleWordClick` - 单词点击事件处理逻辑（需改进）
- 单词清理正则：`/[^a-zA-Z]/g`

### 后端
- `router` - FastAPI 路由器
- `DictionaryResponse` - 词典响应Schema
- 数据提取逻辑（从第三方API解析）

### 缺失组件（需要新增）
- **词形还原工具**（lemmatization）：将 ceilings → ceiling
- **默认词库加载器**：加载和查询预设词汇等级
- **单词高亮/标注工具**：根据词库等级应用颜色

## 4. 测试策略

### 测试框架
- 前端：未找到测试文件（项目可能缺少测试）
- 后端：未找到测试文件

### 测试模式建议
- **单元测试**：词形还原函数、单词清理函数
- **集成测试**：API端到端测试（模拟点击 → API调用 → 显示释义）
- **手动测试**：在阅读页面点击不同形式的单词（原形、复数、过去式）

### 覆盖要求
- 正常流程：点击原形单词（如 ceiling）→ 显示释义
- 边界条件：
  - 点击词形变化（ceilings, went, running）
  - 点击停用词（the, a, and）
  - 点击不规则单词（children, feet）
  - 点击非英文字符
- 错误处理：
  - API超时
  - 网络错误
  - 单词不存在（真正不存在 vs 词形问题）

## 5. 依赖和集成点

### 外部依赖
- **Free Dictionary API**（`https://api.dictionaryapi.dev`）
  - 免费、无需API密钥
  - 仅支持原形单词精确匹配
  - 返回格式：JSON（包含音标、释义、例句、音频）

### 内部依赖
- 前端：
  - React Router（页面导航）
  - Zustand（状态管理）
  - Axios（HTTP客户端）
- 后端：
  - FastAPI（Web框架）
  - httpx（异步HTTP客户端）
  - Pydantic（数据验证）

### 集成方式
- 前端 → 后端：RESTful API（`/api/dictionary/{word}`）
- 后端 → 第三方API：HTTP代理
- 状态管理：Zustand全局store

### 配置来源
- API Base URL：`/api`（由 Vite proxy 转发到后端）
- 超时设置：前端 10000ms，后端 10.0s

## 6. 技术选型理由

### 为什么用第三方API而非自建词典？
**优势**：
- 数据完整性高（包含音标、音频、例句）
- 免维护成本
- 实时更新

**劣势和风险**：
- **仅支持原形单词**（导致当前问题）
- 依赖外部服务稳定性
- 无法离线使用
- 无法定制释义内容

### 为什么用事件委托而非逐词绑定？
**优势**：
- 性能好（单个事件监听器）
- 代码简洁
- 支持动态内容

**劣势**：
- 需要从点击位置提取单词（可能不准确）
- 无法提前标记词性、词形

## 7. 关键风险点

### 当前问题的根本原因（问题1）
**[P0] ceiling 不显示释义的真正原因可能是**：
1. ✅ **不是 API 问题**：外部 API 正常返回 ceiling 的释义（已验证）
2. ⚠️ **可能是前端清理逻辑问题**：
   - 用户点击的可能不是"ceiling"，而是"ceiling."或"ceiling,"
   - 清理正则 `/[^a-zA-Z]/g` 应该能处理标点，但需验证
3. ⚠️ **可能是大小写问题**：
   - 已转小写（`.toLowerCase()`），应该不是问题
4. ⚠️ **可能是空格/特殊字符问题**：
   - HTML渲染后可能包含隐藏字符（如 `&nbsp;`）

**验证步骤**：
1. 在前端 `handleWordClick` 中添加 `console.log(cleanWord)` 查看实际传递的单词
2. 检查后端日志，确认收到的请求参数
3. 测试不同位置的 ceiling（段首、段中、段尾）

### 词形变化问题（潜在P0）
- 当前系统无法处理：
  - 复数形式：ceilings → ceiling
  - 过去式/过去分词：went → go, running → run
  - 不规则变化：children → child
- **解决方案**：集成词形还原库（如 `lemmatizer` Python库，或 `compromise` JS库）

### 默认词库功能的技术挑战（问题2）

#### 性能瓶颈
- 大段文本（1000+ 单词）的逐词标注可能卡顿
- 词库查询效率：需要高效数据结构（Hash Set 或 Trie）

#### 边界条件
- 如何处理大小写？（The vs the）
- 如何处理词形变化？（与问题1相同）
- 如何处理专有名词？（Harry Potter）

#### 数据来源和版权
- 使用现成词表（如 Oxford 3000）需注意版权
- 可考虑使用开源词表（如 COCA 5000、GSL）

## 8. 默认词库功能设计建议

### 词汇分级标准建议
采用 **多词表组合** 方案：
- **基础词表**（500词）：the, a, is, have, can 等
- **常见词表**（3000词）：基于 COCA/GSL
- **进阶词表**（6000词）：中高级词汇
- **学术词表**（AWL）：用于高年级

### 颜色编码方案建议
- 🟢 **绿色**：基础词（已掌握，不需要学习）
- 🟡 **黄色**：常见词（应该认识的）
- 🔴 **红色**：生词（需要学习的）
- ⚪ **无颜色**：超纲词/专有名词

### 数据存储方案
```python
# 数据库新增表
class DefaultVocabulary(Base):
    word = Column(String, primary_key=True)
    level = Column(String)  # 'basic', 'common', 'advanced'
    frequency_rank = Column(Integer)  # 词频排名
```

### 前端渲染优化
```tsx
// 预处理：提取所有单词并批量查询等级
const wordLevels = await api.batchCheckLevel(words);

// 渲染时应用颜色
content.replace(/\b(\w+)\b/g, (word) => {
  const level = wordLevels[word];
  const color = levelColors[level];
  return `<span class="${color}">${word}</span>`;
});
```

## 9. 下一步行动建议

### 立即执行（P0）
1. **诊断 ceiling 问题**：
   - 添加前端日志，查看 `cleanWord` 的实际值
   - 测试不同场景（段首、段尾、带标点）
   - 确认是否真的是"ceiling"还是其他问题

2. **修复词形还原问题**：
   - 后端集成 `lemminflect` 或 `nltk` 词形还原
   - 修改 `lookup_word` API，先还原再查询

### 短期优化（P1）
3. **设计默认词库方案**：
   - 选择开源词表（推荐 COCA 5000 + GSL）
   - 设计数据库Schema
   - 实现批量词汇等级查询API

4. **实现单词标注**：
   - 前端预处理章节内容
   - 应用颜色标注
   - 优化渲染性能

### 长期规划（P2）
5. **离线词典支持**：考虑自建词典数据库，减少对外部API依赖
6. **个性化词库**：根据用户学习进度动态调整颜色标注
7. **词汇学习追踪**：记录用户查询历史，智能推荐需要复习的词汇
